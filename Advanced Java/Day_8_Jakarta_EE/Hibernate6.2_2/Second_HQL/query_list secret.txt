List<Person> mylist1=(List<Person>) query.list();

fires query
gets records from database 
if session contains those objects with the matching records, then it creates a list which contains references to those objects referred by session.

if session does not contain those objects with the matching records, then it creates those many objects , stores their references inside session and then creates a list which contains references to those objects.