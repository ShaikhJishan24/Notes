open pycharm and create RESTful service.

		First.py

from flask import Flask, jsonify

app = Flask(__name__)

@app.route('/api/data', methods=['GET'])
def get_data():
    data = {'message': 'Hello from Python REST service!'}
    return jsonify(data)

if __name__ == '__main__':
    app.run(debug=True)


run this(Run First)  and make sure that python REST service is running on
Werkzeug server on port no. 5000.


now start STS and create one REST service which will invoke above REST service created in Python.

File
	new 
		Spring starter project
			PythonCaller

		Maven      jar
		17         java

	dependencies to be selected
		Spring Web
		Spring boot devtools



		PythonCallerApplication.java


package com.example.demo;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;
import org.springframework.context.annotation.Bean;
import org.springframework.web.client.RestTemplate;

@SpringBootApplication
public class PythonCallerApplication {

	public static void main(String[] args) {
		SpringApplication.run(PythonCallerApplication.class, args);
	}
	
	@Bean
	public RestTemplate getRestTemplate()
	{
		return new RestTemplate();
	}

}


			MyController.java

package com.example.demo;

import java.util.List;

import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.web.bind.annotation.GetMapping;
import org.springframework.web.bind.annotation.RestController;
import org.springframework.web.client.RestTemplate;

@RestController
public class MyController
{
	@Autowired
	private RestTemplate template;
	
	@GetMapping("/api/callPython")
	public List<?> getData()
	{
		return template.getForObject("http://127.0.0.1:5000/api/getmodulelist", List.class);
	}
}


notice in the MyController
		http://127.0.0.1:5000/api/getmodulelist
to call Python REST api from Java REST api.



run PythonCallerApplication
	make sure server starts successfully

go to postman and type
	http://localhost:8080/api/callPython
make sure "GET"  is selected

click on "send"
	make sure it gives u following output.

[
    "java",
    "MySQL",
    "Python",
    "C++"
]



